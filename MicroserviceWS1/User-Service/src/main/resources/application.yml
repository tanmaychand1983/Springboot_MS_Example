server:
  port: 8042
  
spring:  application:    name: USER-SERVICE
  
eureka:  client:    register-with-eureka: true
    fetch-registry: true
    service-url:      defaultZone: http://localhost:8040/eureka
  instance:
    hostname: localhost
     
          